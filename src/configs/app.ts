import { Snackbar } from "../utils/types"

export const HEADER = {
    DESKTOP_HEIGHT: 64,
}

export const NAVBAR = {
    HEIGHT: 48,
}

export const SNACKBAR: Snackbar = {
    HIDE_DURATION: 3000,
    ANCHOR_VERTICAL: "top",
    ANCHOR_HORIZONTAL: "center",
}
